import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'todo-list-sveltekit',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
